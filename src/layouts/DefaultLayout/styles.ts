import { styled } from 'styled-components'

export const ContentContainer = styled.main`
  margin-top: 18.5rem;
  position: absolute;
  width: 100%;

  @media (max-width: 960px) {
    padding: 0 1.875rem;
  }
`
